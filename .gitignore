# Binaries — match only the compiled artifacts, not the cmd/notifyd source
# directory (which `go build ./cmd/notifyd` produces a `/notifyd` binary
# from at the repo root).
/bin/
/notifyd
*.exe

# Test / coverage output
*.out
*.test
coverage*

# Local env
.env
*.local

# Editor / OS
.DS_Store
.idea/
.vscode/

# docs-site build artifacts
docs-site/node_modules/
docs-site/dist/
docs-site/.astro/
